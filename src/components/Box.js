import styled from 'styled-components';

import {
  color,
  space,
  layout,
  flexbox,
  grid,
  background,
  border,
  position,
  display,
} from 'styled-system';

export const Box = styled('div')({
  display,
  color,
  space,
  layout,
  flexbox,
  grid,
  background,
  border,
  position,
});
